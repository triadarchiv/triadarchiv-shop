# WICHTIG: Diese Dateien dürfen NIE öffentlich hochgeladen werden!

# Geheimer Stripe-Schlüssel (liegt online in Netlifys geschützten Einstellungen)
stripe-secret.txt

# Geheimes PayPal-Secret (liegt online in Netlifys geschützten Einstellungen)
paypal-secret.txt

# Lokaler PowerShell-Server & Starter (werden online nicht gebraucht)
server.ps1
Shop starten.bat
Online setzen.bat

# Lokale Daten aus dem Testbetrieb
orders.txt
newsletter.txt

# Werkzeug-/Systemordner
.claude/
node_modules/

# Alte/unbenutzte Bilder, nur lokal als Ablage
archiv/

# Admin starten.bat ist nur ein lokaler Helfer
Admin starten.bat
